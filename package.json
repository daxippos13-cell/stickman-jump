{
  "name": "stickman-city-runner",
  "version": "1.0.0",
  "description": "A 3D Stickman Runner Game",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx shx cp -R dist/* . && git add . && git commit -m 'Deploy to GitHub Pages' && git push origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "shx": "^0.4.0",
    "vite": "^5.0.0"
  }
}
